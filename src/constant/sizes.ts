import {Dimensions} from 'react-native';

const {height, width} = Dimensions.get('screen');
const sizes = {
  screem_width: width,
  screem_height: height,
  radius: 10,
  margin: 16,
  base: 6,
  padding: 10,
};

export default sizes;
